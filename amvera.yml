meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "8.0"

build:
  image: null

run:
  image: null
  buildFileName: OtpravkaApi/OtpravkaApi.csproj
  persistenceMount: /data
  containerPort: "80"
