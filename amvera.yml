meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "8.0"

build:
  image: null

run:
  image: null
  buildFileName: OtpravkaApi.sln
  persistenceMount: /data
  containerPort: "80"
  serviceType: compute
